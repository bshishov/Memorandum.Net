{% extends 'file_layout' %}

{% block file_content %}
<div class="image-file center-align">
    <div>
        <img class="responsive-img" src="{{Item.URL}}?action=raw" data-caption="{{Item.Name}}">
    </div>
</div>
{% endblock %}